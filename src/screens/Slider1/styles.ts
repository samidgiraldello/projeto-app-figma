import { SyteSheet } from 'react-native';
import { colors } from '../../styles/color';
import const styles = Styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    panel: {
        flex: 1,
        marginTop: 40,
        margin: 20,
        borderRadius: 20,
        backgroundColor: colors.white
    },
    buttonSlider: {
        flexDirection: "row",
        justifyContent: 'space-around',
        marginBottom: 20
    }
});
