import { StylesSheet } from 'react-native';

export const styles = StylesSheet.create({
    listMarker:{
        margin: 10,
        marginTop: 60,
        flexDirection: 'row',
        alignItems: 'center'
    },
    textMarker:{
        marginLeft: 5,
        fontSize: 20
    }
})