import { StylesSheet } from 'react-native';

export const styles = StyleSheet.create({
    title:{
        fontSize: 40,
        marginTop: 20,
        margin: 10,
        textAling: 'center'
    },
})